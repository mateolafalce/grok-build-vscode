# Repository Instructions

Import the repository instructions from `CLAUDE.md` instead of duplicating them.

If instruction imports are unavailable, ask the user for permission to read `CLAUDE.md`
before proceeding.

Every commit subject follows Conventional Commits. The allowed types, the subject
shape, and the CI job that enforces them are in `CLAUDE.md` (§ Repo conventions,
`checkCommitMessage`).
